# Conditional akd if/else Statements

```
if (condition){
	cout >> result ;
} else (optional condition)
	cout >> result ;
}
```

### Single Line aka Ternary Operation
```
variable = (condition) ? expressionTrue : expressionFalse;
```